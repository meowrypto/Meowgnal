<Window x:Class="Meowgnal.Views.OnboardingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Welcome to Meowgnal" Width="470" SizeToContent="Height"
        WindowStyle="None" WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        FontFamily="Trebuchet MS" FontSize="12" ResizeMode="NoResize">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" ResizeBorderThickness="6"
                      GlassFrameThickness="0" CornerRadius="0"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundBrush" Color="{DynamicResource ColorBackground}"/>
        <SolidColorBrush x:Key="PanelBg" Color="{DynamicResource ColorPanel}"/>
        <SolidColorBrush x:Key="BorderColor" Color="{DynamicResource ColorBorder}"/>
        <SolidColorBrush x:Key="TextColor" Color="{DynamicResource ColorTextPrimary}"/>
        <SolidColorBrush x:Key="TextSecondary" Color="{DynamicResource ColorTextSecondary}"/>
        <SolidColorBrush x:Key="TextMuted" Color="{DynamicResource ColorTextMuted}"/>
        <SolidColorBrush x:Key="AccentColor" Color="{DynamicResource ColorAccent}"/>
        <SolidColorBrush x:Key="DangerColor" Color="{DynamicResource ColorDown}"/>
        <SolidColorBrush x:Key="SuccessColor" Color="{DynamicResource ColorUp}"/>

        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#363A45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource PanelBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextColor}"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource PanelBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
        </Style>
        <Style x:Key="BaseButton" TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom title bar: closing here keeps the old "decline onboarding" behavior -->
            <Border Grid.Row="0" Background="{DynamicResource PanelBg}"
                    BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                        <Button Content="✕" Width="46" Height="32" Style="{StaticResource CloseButton}"
                                Click="Close_Click" WindowChrome.IsHitTestVisibleInChrome="True"/>
                    </StackPanel>
                    <TextBlock Text="🐱 Welcome to Meowgnal" Foreground="{DynamicResource TextColor}"
                               FontWeight="Bold" FontSize="13" VerticalAlignment="Center" Margin="14,0,0,0"/>
                </DockPanel>
            </Border>

            <StackPanel Grid.Row="1" Margin="28">
                <TextBlock Text="🐱 Welcome to Meowgnal" FontSize="20" FontWeight="Bold" Margin="0,0,0,4"/>
                <TextBlock Text="How would you like to start?" Foreground="{DynamicResource TextMuted}"
                           FontSize="12" Margin="0,0,0,18"/>

                <Border Background="{DynamicResource PanelBg}" CornerRadius="6" Padding="16" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Create a local profile" FontWeight="Bold" FontSize="13" Margin="0,0,0,4"/>
                        <TextBlock Text="Stored only on this PC (encrypted). No email, no cloud."
                                   Foreground="{DynamicResource TextMuted}" FontSize="11" Margin="0,0,0,10"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <StackPanel Margin="0,0,10,0">
                                <TextBlock Text="Your name" Foreground="{DynamicResource TextMuted}" FontSize="11" Margin="0,0,0,4"/>
                                <TextBox x:Name="NameBox" Text="Trader" Width="180"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Avatar" Foreground="{DynamicResource TextMuted}" FontSize="11" Margin="0,0,0,4"/>
                                <ComboBox x:Name="AvatarCombo" Width="90" SelectedIndex="0">
                                    <ComboBoxItem Content="🐱"/>
                                    <ComboBoxItem Content="🦊"/>
                                    <ComboBoxItem Content="🐼"/>
                                    <ComboBoxItem Content="🦁"/>
                                    <ComboBoxItem Content="🐸"/>
                                    <ComboBoxItem Content="🚀"/>
                                    <ComboBoxItem Content="🌙"/>
                                    <ComboBoxItem Content="⚡"/>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>
                        <Button Content="Start with my profile" Style="{StaticResource PrimaryButton}"
                                Padding="14,7" HorizontalAlignment="Left" Click="CreateProfile_Click"/>
                    </StackPanel>
                </Border>

                <TextBlock Text="— or —" Foreground="{DynamicResource TextMuted}" FontSize="11"
                           HorizontalAlignment="Center" Margin="0,0,0,12"/>

                <Border Background="{DynamicResource PanelBg}" CornerRadius="6" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Continue as guest" FontWeight="Bold" FontSize="13" Margin="0,0,0,4"/>
                        <Button Content="Continue as guest" Style="{StaticResource SecondaryButton}"
                                Padding="14,7" HorizontalAlignment="Left" Click="Guest_Click"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Border>
</Window>