<Window x:Class="Meowgnal.Views.DisciplineReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Discipline Report" Height="520" Width="600"
        WindowStyle="None" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        FontFamily="Trebuchet MS" FontSize="12"
        ResizeMode="NoResize">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" ResizeBorderThickness="6"
                      GlassFrameThickness="0" CornerRadius="0"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundBrush" Color="{DynamicResource ColorBackground}"/>
        <SolidColorBrush x:Key="PanelBg" Color="{DynamicResource ColorPanel}"/>
        <SolidColorBrush x:Key="BorderColor" Color="{DynamicResource ColorBorder}"/>
        <SolidColorBrush x:Key="TextColor" Color="{DynamicResource ColorTextPrimary}"/>
        <SolidColorBrush x:Key="TextMuted" Color="{DynamicResource ColorTextMuted}"/>
        <SolidColorBrush x:Key="AccentColor" Color="{DynamicResource ColorAccent}"/>
        <SolidColorBrush x:Key="Up" Color="{DynamicResource ColorUp}"/>
        <SolidColorBrush x:Key="Down" Color="{DynamicResource ColorDown}"/>

        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMuted}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#363A45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource PanelBg}"
                    BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                        <Button Content="✕" Width="46" Height="32" Style="{StaticResource CloseButton}" Click="Close_Click"
                                WindowChrome.IsHitTestVisibleInChrome="True"/>
                    </StackPanel>
                    <TextBlock Text="📊 Discipline Report" Foreground="{DynamicResource TextColor}"
                               FontWeight="Bold" FontSize="13" VerticalAlignment="Center" Margin="14,0,0,0"/>
                </DockPanel>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
                <StackPanel>
                    <TextBlock x:Name="HeaderTitle" Text="How your discipline affects your results"
                               FontSize="16" FontWeight="SemiBold" Margin="0,0,0,14"/>

                    <UniformGrid Columns="2" Margin="0,0,0,12">
                        <Border Background="{DynamicResource PanelBg}" CornerRadius="6" Padding="14,12" Margin="0,0,8,0">
                            <StackPanel>
                                <TextBlock Text="✓ Checklist completed" Foreground="{StaticResource Up}" FontSize="11" FontWeight="SemiBold"/>
                                <TextBlock x:Name="CompletedWinRateText" Text="—" FontSize="26" FontWeight="Bold" Margin="0,4,0,0"/>
                                <TextBlock x:Name="CompletedCountText" Text="—" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Background="{DynamicResource PanelBg}" CornerRadius="6" Padding="14,12" Margin="4,0,0,0">
                            <StackPanel>
                                <TextBlock Text="⚠ Skipped or critical flagged" Foreground="{StaticResource Down}" FontSize="11" FontWeight="SemiBold"/>
                                <TextBlock x:Name="SkippedWinRateText" Text="—" FontSize="26" FontWeight="Bold" Margin="0,4,0,0"/>
                                <TextBlock x:Name="SkippedCountText" Text="—" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </UniformGrid>

                    <Border x:Name="NoDataBanner" Background="#3D1A1A" CornerRadius="6" Padding="12" Margin="0,0,0,12" Visibility="Collapsed">
                        <TextBlock Foreground="#F5E5E5" FontSize="12" TextWrapping="Wrap"
                                   Text="🐾 Not enough data yet — keep trading (with the checklist) to unlock this report. You need at least 10 completed and 10 skipped trades."/>
                    </Border>

                    <Border x:Name="PatternPanel" Background="{DynamicResource PanelBg}" CornerRadius="6" Padding="14" Margin="0,0,0,12" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="Behavioral patterns detected" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <TextBlock x:Name="TimePatternText" TextWrapping="Wrap" Margin="0,0,0,6" Visibility="Collapsed"/>
                            <TextBlock x:Name="PostLossPatternText" TextWrapping="Wrap" Visibility="Collapsed"/>
                        </StackPanel>
                    </Border>

                    <Border Background="{DynamicResource PanelBg}" CornerRadius="6" Padding="14">
                        <StackPanel>
                            <TextBlock Text="Summary" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <TextBlock x:Name="SummaryText" TextWrapping="Wrap" Foreground="{StaticResource TextColor}" FontSize="12"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>