<UserControl x:Class="Meowgnal.Views.FibLevelsEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="LevelsList" Grid.Row="0" Margin="0,0,0,8" BorderThickness="1" BorderBrush="{DynamicResource BorderPrimary}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <CheckBox Grid.Column="0" IsChecked="{Binding Enabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Grid.Column="1" Text="{Binding Ratio, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=F3}" VerticalAlignment="Center" Margin="0,0,8,0" Padding="4,2"/>
                        <ComboBox Grid.Column="2" SelectedValue="{Binding Color, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Tag" Margin="0,0,8,0" Width="50">
                            <ComboBoxItem Content="■" Foreground="#2962FF" Tag="#2962FF"/>
                            <ComboBoxItem Content="■" Foreground="#EF5350" Tag="#EF5350"/>
                            <ComboBoxItem Content="■" Foreground="#26A69A" Tag="#26A69A"/>
                            <ComboBoxItem Content="■" Foreground="#FF9800" Tag="#FF9800"/>
                            <ComboBoxItem Content="■" Foreground="#9C27B0" Tag="#9C27B0"/>
                            <ComboBoxItem Content="■" Foreground="#B2B5BE" Tag="#B2B5BE"/>
                        </ComboBox>
                        <TextBox Grid.Column="3" Text="{Binding Label, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,0,8,0" Padding="4,2"/>
                        <Button Grid.Column="4" Content="🗑" Click="RemoveLevel_Click" CommandParameter="{Binding}" Padding="6,2" FontSize="12" Background="Transparent" BorderThickness="0" Cursor="Hand"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Grid.Row="1" Content="+ Add Level" Click="AddLevel_Click" HorizontalAlignment="Left" Padding="12,6" Background="{DynamicResource BgPanel}" Foreground="{DynamicResource TextPrimary}" BorderThickness="1" BorderBrush="{DynamicResource BorderPrimary}"/>
    </Grid>
</UserControl>